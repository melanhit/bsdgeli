Index: sys/geom/eli/g_eli_hmac.c
===================================================================
--- sys/geom/eli/g_eli_hmac.c	(revision 313496)
+++ sys/geom/eli/g_eli_hmac.c	(working copy)
@@ -131,6 +131,7 @@
 
 	switch (sc->sc_ealgo) {
 	case CRYPTO_AES_XTS:
+	case CRYPTO_THREEFISH_XTS:
 		bcopy(off, iv, sizeof(off));
 		bzero(iv + sizeof(off), size - sizeof(off));
 		break;
